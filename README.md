# My Course Archive (GitHub Pages)

## Edit content
Update `_data/modules.yml` to add topics, lectures, and links.

## Publish
Repo Settings → Pages → Deploy from a branch → main / (root)

## Notes about file sizes
- GitHub Pages published sites are limited to 1 GB.
- Individual git objects are enforced at 100 MB (use Git LFS if needed).
